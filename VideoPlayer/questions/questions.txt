2022-08-26
000.在本地使用msvc64位编译器，debug和release都能正常使用，但是打包以后，连接不上数据库；
提示：驱动未加载；
解决办法：首先看你连接的数据库是什么版本，多少位。而后下载对应的版本位数，将其中的两个（实际上正是运行）
只需要release版本的，不过debug的时候，也是需要，所以还是顺便复制过来。（https://downloads.mysql.com/archives/community/）
